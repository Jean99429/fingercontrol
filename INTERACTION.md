# Interaction Direction

## 项目边界

fingercontrol 是一次性的实时视觉拍摄工具，不是作品集网站中的线上摄像头功能。

制作链路：

1. 摄像头捕捉 Jean。
2. MediaPipe 识别双手与手指关键点。
3. 浏览器实时触发文字、声音和 WebGL 视觉效果。
4. 使用 OBS 或系统录屏录制表演。
5. 剪辑并导出循环视频。
6. 作品集网站只负责播放最终视频，并可增加轻微鼠标反馈。

## 交互语法

### 指尖识别层

- MediaPipe 内部可继续读取完整手部关键点以保证识别稳定。
- 最终画面只绘制五个指尖红点。
- 不绘制手腕、掌心框或完整骨骼。
- 拇指接近某根手指时，两点之间出现细线。
- 捏合保持约 180–250ms 后才确认触发，避免抖动。
- 触发成功后两点短暂合并，并产生声音或视觉反馈。

### 左手：内容与声音

- 左手有四个内容插槽，分别绑定食指、中指、无名指和小指。
- 每个插槽可以填写自定义文字。
- 每个插槽可以启用浏览器 TTS，设置语速和音量，或绑定自定义声音文件。
- 设置页允许调整四个内容插槽与四根手指的映射关系。
- 文字从捏合点出现；保持捏合时可以随手移动；松开后停留或消散。

具体文案暂未确定，制作前再锁定。

### 右手：视觉效果

- 右手四根手指不是四套可编辑 Shader，而是四个效果触发槽位。
- 每个槽位从固定效果库中选择一个效果，并绑定到食指、中指、无名指或小指。
- 设置页只允许修改“哪根手指触发哪个效果”。
- 效果的视觉参数、运动方式、颜色、强度曲线和恢复节奏均由项目代码预先设计并写死。
- 不向拍摄者开放粒子数量、大小、速度、方向、颜色或 Shader 参数调节。
- 张开手掌：恢复默认状态。

初始固定效果库：

1. PARTICLE DISASSEMBLY — 人物转为粒子并解构。
2. ASCII — 人物表面转为字符。
3. DITHER — 人物进入抖动网点状态。
4. RGB TIME ECHO — RGB 分离与时间拖影。
5. DATA SLICE — 人物被水平切片、错位和压缩。
6. PIXEL SORT — 像素沿固定方向拉伸和排序。
7. DOT MATRIX — 人物转为规则点阵。
8. GLYPH DISSOLVE — 人物先转为字符材料，再逐步消散。
9. NEGATIVE THRESHOLD — 黑白反相与阈值扫描。
10. OPTICAL DISTORTION — 局部折射、模糊与形变。

上述效果是内置成品，不是让使用者自行创建效果。可以继续增加新的固定效果，但新增效果必须在代码中完成、测试并加入效果库。

部分效果可在其内部预先规定手部运动对动画的影响，例如左右移动改变拖影方向；这种映射属于效果实现本身，也不在设置页开放编辑。

人物类效果需要人物分割遮罩，避免默认把背景和人物一起处理。只有明确设计为全画面效果的模式才作用于完整画面。

### 右手效果映射示例

- 拇指 + 食指：PARTICLE DISASSEMBLY。
- 拇指 + 中指：ASCII。
- 拇指 + 无名指：RGB TIME ECHO。
- 拇指 + 小指：DATA SLICE。

这只是默认分配。Jean 可以在设置页把四个已选效果重新分配给四根手指，但不能在设置页修改效果内部参数。

## 推荐最终镜头

工具可支持完整手势集合，但首屏成片先控制在三个动作：

1. 左手捏出一个短词并放到画面中央。
2. 右手让人物进入粒子状态。
3. 双手将文字与人物一起拆散，松开后重新聚合，回到循环起点。

## 视觉原则

- 近黑背景、灰白人物、红色指尖与触发线。
- 追踪信息服务于动作，不做满屏技术 HUD。
- 不沿用参考网站的默认文字与点样式。
- 人物是主视觉，识别标记只是动作发生的证据。

## 后续待定

- 左手四个手势对应的最终文字。
- 是否使用 Jean 的人声、音乐或纯交互声音。
- 第一版从固定效果库中实际实现哪四个效果。
- 第一版四个效果与右手四根手指的默认映射。
- 拍摄背景、服装、构图和视频循环节奏。
- 实现方式与生成工具在上述内容确定后再选择。

## 实现与开源来源原则

- 优先查找成熟的开源 WebGL、GLSL、Three.js 或 Canvas 实现作为技术参考。
- 使用前核对许可证、署名要求和商业使用条件。
- 将采用的来源和许可证记录在项目中，不直接复制来源不明的演示代码。
- Gemini 或其他生成工具只帮助搭建和改写代码；fingercontrol 的运行不调用 Gemini API。
