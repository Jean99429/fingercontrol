# Interaction Direction

## 项目边界

fingercontrol 不再负责生成 ASCII、粒子或其他视觉效果。它提供 `CAMERA` 与 `UPLOAD VIDEO` 两种输入模式，两种模式都只负责识别手势、叠加文字和使用与 Fingertalk 相同的浏览器语音直接朗读单词。

上传视频链路：

1. 上传任意本地视频。
2. 如果上传视频经过重度风格化并影响识别，可同时上传同源原视频作为隐藏识别轨。
3. fingercontrol 分析左右手捏合事件。
4. 在事件时间点叠加文字和对应音频。
5. 检查预览并导出最终视频。

实时摄像头链路：

1. 选择 `CAMERA`。
2. 请求摄像头权限并选择设备。
3. 实时识别双手捏合动作。
4. 在正常摄像头画面上实时叠加文字、指尖点和双手坐标框。
5. 触发时直接朗读对应输入框的单词。
6. 可在浏览器中录制并导出，或使用系统录屏。

## 双手统一交互

- 左右手使用完全相同的触发机制。
- 拇指只作为触发器，不对应内容输入框。
- 拇指分别接触食指、中指、无名指和小指。
- 每只手有四个固定文字输入框，共八个单词。
- 每行只输入文字，不上传音频文件。
- 不再区分“左手控制文字、右手控制视觉效果”。
- 单次捏合只触发一次；必须松开并经过冷却后才能再次触发。

## 输入模式

### CAMERA

- 显示正常、原色、未加滤镜的实时摄像头画面。
- 不添加任何旧版视觉效果。
- 实时运行同一套双手识别、文字与音频逻辑。

### 显示视频（必选）

- 接受浏览器可以解码的任意本地视频。
- 它是预览和最终导出的视觉底片。
- fingercontrol 原样使用视频画面，不添加视觉滤镜。

### 识别视频（可选）

- 使用与显示视频严格同步、较容易识别手部的原视频。
- 只在后台进行手势识别，不出现在最终画面中。
- 如果未上传，则直接分析显示视频。

## 追踪视觉

- 内部读取完整 21 个手部关键点。
- 最终画面只显示双手五个指尖点和双手坐标框，不显示骨骼。
- 指尖点必须比旧版明显更大。
- 使用正红色 `#FF0000`，禁止偏橙、偏粉或暗红。
- 双手都显示自己的坐标框。
- 坐标框比检测到的手部范围略大，使用克制的角标、短 X/Y 轴和 `L`/`R` 标签。
- 捏合确认时，拇指点、目标手指点和捏合中心短暂放大。

## 文字与声音

- 文字从拇指与目标手指的接触中点附近出现。
- 文字短暂跟随手部位置，随后停留并淡出。
- 完整复制 Fingertalk 的浏览器 Speech Synthesis 语音逻辑，不调用 Gemini。
- 使用系统英文声音池，优先 Samantha、Karen、Moira、Tessa、Victoria、Allison、Ava、Susan、Zoe、Kate、Serena、Fiona，以及 Alex、Daniel、Aaron、Fred、Tom、Arthur、Oliver、Gordon、Nathan、Rishi、Lee。
- 八个单词按顺序交替分配女性和男性声音，并使用 Fingertalk 相同的 pitch 与 rate 变化。
- 点击 `START CAMERA` 或 `ANALYZE VIDEO` 时先执行 `speechSynthesis.resume()`，刷新 voices，并播放一个空白 utterance 解锁语音。
- 从“未捏合”进入“捏合”的瞬间直接朗读，450ms 内不重复触发。
- 朗读新单词前先 `speechSynthesis.cancel()`，避免队列积压。
- 不播放提示音、点击音或效果音。

## 设置页视觉方向

参考 Jean 提供的 Fingertalk 截图，只采用其信息结构，不复制品牌、韩文或装饰：

- 深色、安静的全屏背景。
- 页面内容集中在画面中央。
- 顶部只有项目名和一句简短说明。
- 中部只有两个并排的大框：左手在左、右手在右。
- 每个框固定四行：食指、中指、无名指、小指。
- 每行只有手指名称和一个文字输入框，与 Fingertalk 一样直接。
- 不增加手指映射、效果选择、声音模式、语速或音量等设置。
- 顶部使用简洁的 `CAMERA / UPLOAD VIDEO` 二选一切换。
- 底部只有一个主按钮；摄像头模式显示 `START CAMERA →`，上传模式显示 `ANALYZE VIDEO →`。
- 不使用游戏 HUD、渐变发光卡片、机器人贴纸或多层工具栏。

## 预览页

- 视频是画面主体。
- 只保留返回设置、重新分析、播放/暂停和导出。
- 检测事件以很轻的时间轴标记显示，方便发现误触。
- 文字、追踪点、坐标框和声音必须进入最终导出结果。
