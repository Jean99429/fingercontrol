# Interaction Direction

更新日期：2026-09-08

## 1. SETUP

- 深色全屏背景，两只张开的手是页面视觉核心。
- 左手由右手素材水平镜像生成，确保两只手形状一致。
- 八根非拇指直接承载可编辑文字；输入框无外框、无下划线、无暗色底和旋转角度。
- 文字水平居中在对应手指上，始终保持清晰可读。
- 拇指没有文字与编辑能力，只显示红色触发点。
- 标题使用 “Pinch a finger. Make it speak.”，说明用户先分配文字，再用拇指触碰手指触发声音。
- CAMERA 主按钮为 `START PERFORMING`；UPLOAD VIDEO 模式为 `ANALYZE VIDEO`。
- 摄像头、视频文件、镜像和追踪设置收纳在底部紧凑控制区。

## 2. LIVE / PREVIEW

- 原始摄像头或上传视频是画面主体，不添加图像滤镜。
- 检测到手后，四根非拇指的文字始终显示在对应指尖附近。
- 文字不因捏合结束而消失；捏合只负责触发声音和交互反馈。
- 内页文字直接共用设置页的内容、字体、字重、斜体与颜色。
- 控制区使用与首页一致的深色表面、浅色文字、细边框和薄荷绿状态色。
- 控制仅保留返回、声音测试、追踪、镜像、录制/导出，以及上传模式所需的播放与事件操作。

## 3. 默认文字视觉

| 位置 | 文字 | 字体 | 样式 | 颜色 |
| --- | --- | --- | --- | --- |
| 左小指 | FLOW | Instrument Serif | 400 Italic | `#C77CFF` |
| 左无名指 | VECTOR | Unbounded | 800 | `#E65100` |
| 左中指 | PULSE | Caveat | 700 | `#3CD9FF` |
| 左食指 | SIGNAL | Archivo Black | 900 | `#FF5A5A` |
| 右食指 | OBJECT | DM Mono | 500 | `#FF5A5A` |
| 右中指 | MOTION | Space Grotesk | 700 | `#FFB300` |
| 右无名指 | SPACE | Bricolage Grotesque | 800 | `#3CD9FF` |
| 右小指 | TIME | Playfair Display | 600 Italic | `#5CFFB0` |

## 4. 手势与追踪

- 左右手遵循同一套规则：拇指分别接触食指、中指、无名指和小指。
- 单次闭合只触发一次；分开后允许下一次触发。
- 同一只手同一时刻只接受一个最近的目标手指。
- 五个指尖追踪点固定使用 `#E60340`；首页拇指点与内页追踪点保持一致。
- 红色仅承担手指追踪语义，不作为按钮或普通状态的主强调色。
- 技术追踪框与坐标信息由 `TRACKING` 控制，文字常显不受该开关影响。

## 5. 颜色与排版

- 页面背景：`#101217`。
- 次级表面：`#15171C` / `#1C1E24`。
- 边框：`#303239`。
- 主文字：`#F6F1E8`。
- 次要文字：`#92949A`。
- 主强调色：薄荷绿 `#5CFFB0`。
- 手指追踪点：信号红 `#E60340`。
- 界面避免大面积红色、厚重描边、游戏 HUD 和多余说明。

## 6. 语音与导出

- 任意非空文字都可以触发语音。
- 八个槽循环使用 `af_heart`、`am_puck`、`af_sarah` 三种 Kokoro 音色。
- 预览优先使用系统英文语音，最终导出使用可混入 Web Audio 的 Kokoro 音频。
- 上传模式保留事件时间轴、误触删除、重新分析与 MP4 保存流程。
