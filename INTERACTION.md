# Interaction Direction

## 两个页面

### 1. SETUP

- 深色、简洁的中央布局。
- 顶部选择 `CAMERA` 或 `UPLOAD VIDEO`。
- 左右两个等宽面板，各有 INDEX、MIDDLE、RING、PINKY 四个文字输入框。
- 拇指没有输入框，只负责触发。
- 上传模式接受显示视频，并可选同源干净识别视频。
- 页面不提供效果选择、校准、声音选择或音频文件上传。

### 2. LIVE / PREVIEW

- 视频是画面主体。
- CAMERA 显示原始摄像头；UPLOAD VIDEO 原样使用显示视频。
- 控制仅保留返回、播放/暂停、追踪开关、镜像、重新分析和下载。
- 上传分析完成后可查看事件并删除明显误触。
- 下载流程显示 `PREPARING AUDIO…`、`EXPORTING…`，完成后提供持久的 `SAVE VIDEO`。

## 手势

- 左右手完全相同。
- 拇指分别接触四个非拇指指尖。
- 单次闭合只触发一次；分开后文字立即消失。
- 同一只手同一时刻只接受一个最近的目标手指。
- 过滤短于 120ms 的上传视频噪声事件，避免同一次捏合闪出错误单词。

## 追踪视觉

- 五个指尖点常显，颜色 `#E60340`，无黑色描边，尺寸清晰可见。
- 坐标标签常显：半透明灰底、白色终端等宽字体。
- 手部框不常显；只与触发文字同时出现。
- 触发框为白色细线，不加重影、拖尾或释放缓冲。
- 所有点、坐标和框直接使用当前识别帧位置，避免额外平滑造成延迟。

## 文字

- 在捏合中心附近出现。
- 全大写。
- 白色终端代码字体。
- 统一 `#E60340` 底色并保留少量 padding。
- 无黑色描边、无标题、无附加状态文字、无鬼影。
- 手指分开后立即消失。

## 语音

- 任意输入文字都可以发声。
- 所有八个槽循环使用三个最终选定的 Kokoro 音色：
  1. `af_heart`（来自当前 JEAN 的音色）
  2. `am_puck`（来自当前 I 的音色）
  3. `af_sarah`（来自当前 AM 的音色）
- 预览优先使用可用的浏览器英文系统语音以即时响应；不可用时自动回退到 Kokoro。
- 最终导出使用可进入 Web Audio 混音的 Kokoro 音频。
- Kokoro 不在页面启动时加载，只在需要时动态加载。

## 导出

- 显示视频决定最终时长、尺寸和画面。
- 识别视频只辅助手指定位，不因时长差异改变显示主轨。
- 合成显示视频原音轨、文字/追踪 Canvas 和触发语音。
- 只使用浏览器实际支持的 MP4 MediaRecorder 类型。
- 文件扩展名固定 `.mp4`；不回退或伪装为其他容器。
- 自动下载被拦截时，页面保留 `SAVE VIDEO` 供用户主动保存。
