# fingercontrol — Product & Technical Specification

状态：需求规格 v1  
项目路径：`/Users/jean/Documents/ChatGPT/fingercontrol`  
归属：独立工具，不属于 `portfolio WEB OF JEAN`

本文档是 fingercontrol 的唯一权威规格。AI Studio 提示词、代码实现、视觉审核与验收均以本文为准。

## 1. 产品定义

fingercontrol 是一个桌面端实时摄像头手势视觉拍摄工具。Jean 在镜头前使用双手完成一段实时视觉表演，然后通过 OBS、系统录屏或后续内置录制功能生成视频。最终作品集网站只播放剪辑后的成片，不向网站访客请求摄像头权限，也不运行手势识别。

### 1.1 目标

- 稳定识别左右手的拇指与四根手指捏合。
- 左手触发自定义文字与声音。
- 右手触发固定实现的视觉效果。
- 只显示指尖追踪语言，不显示完整技术骨骼。
- 输出具有 TouchDesigner 气质、但属于 Jean 自己视觉系统的可录制画面。
- 将复杂度放在一次性的拍摄工具里，使作品集首屏保持低成本、稳定的视频播放。

### 1.2 非目标

- 不是作品集线上互动功能。
- 不需要账号、数据库、服务器或多人功能。
- 运行时不调用 Gemini API。
- 不训练自定义机器学习模型。
- 不在设置页制作 Shader、节点图或任意效果编辑器。
- 不提供手势校准页面。
- 不显示手腕、掌心框或完整手部骨骼。

## 2. 技术架构

```text
Camera
  ├─ MediaPipe Hand Landmarker → 左右手、21点坐标、捏合状态
  ├─ Person Segmentation       → 人物遮罩
  └─ Mirrored Video Texture    → WebGL / Three.js

Gesture State Machine
  ├─ Left Hand  → Text / TTS / Audio
  └─ Right Hand → Fixed Effect Library

Renderer
  ├─ Camera / person layer
  ├─ Effect layer
  ├─ Fingertip tracking layer
  └─ Text layer
```

### 2.1 技术栈

- React
- TypeScript
- MediaPipe `@mediapipe/tasks-vision`
- MediaPipe Hand Landmarker，`runningMode: VIDEO`，`numHands: 2`
- MediaPipe 人物分割方案，用于生成实时人物遮罩
- Three.js 或原生 WebGL / GLSL
- Web Audio API 与浏览器 Speech Synthesis
- localStorage
- 目标平台：桌面 Chrome

### 2.2 AI Studio 特殊要求

Google AI Studio Build 只用于生成和迭代第一版代码。项目本身是纯客户端工具。

`metadata.json` 必须声明：

```json
{
  "name": "fingercontrol",
  "requestFramePermissions": ["camera"]
}
```

若后续使用麦克风录制，才增加 `microphone`；第一版不申请麦克风权限。

### 2.3 性能原则

- 优先保证摄像头、双手识别和主画面达到稳定视觉帧率。
- 手部识别和人物分割不必与渲染使用相同帧率。
- 手部模型推理应与渲染解耦；必要时使用 Web Worker，避免同步推理阻塞 UI。
- 第一目标分辨率为 1920×1080；性能不足时内部效果可以低分辨率渲染后放大。
- 手离开画面、模型暂时丢失或置信度不足时，效果平滑恢复，不允许闪屏。

## 3. 应用结构

应用是一个单页应用，包含两个主状态：

1. `CONTROL SETUP`
2. `PERFORMANCE`

不增加独立欢迎页、教程页或校准页。

## 4. SCREEN 1 — CONTROL SETUP

### 4.1 页面结构

- 近黑色全屏背景。
- 页面主体为左右两列。
- 左列：左手文字与声音插槽。
- 右列：右手视觉效果映射。
- 底部主按钮：`ENABLE CAMERA →`。
- 使用简洁表格、下拉框和输入框，不使用发光卡片、蓝紫渐变或游戏 HUD。

### 4.2 左手：四个可配置内容插槽

四个插槽分别对应食指、中指、无名指和小指。拇指只作为触发器，不是可绑定对象。

每个插槽包含：

- 启用/停用。
- 触发手指选择：INDEX / MIDDLE / RING / PINKY。
- 自定义文字输入。
- 声音模式：OFF / BROWSER TTS / AUDIO FILE。
- TTS 声音选择。
- TTS 语速。
- TTS 音量。
- TTS 预览按钮。
- 自定义音频文件选择与预览；文件仅在本地当前会话使用，不上传服务器。

约束：

- 同一根手指同一时间只能绑定一个左手插槽。
- 更换映射时若发生重复，界面必须提示并交换映射或阻止保存。
- 默认文字使用 `TEXT 01` 至 `TEXT 04` 占位，不擅自填入最终个人文案。
- Jean 的最终文案不属于程序固定内容，可以随时在设置页修改。

### 4.3 右手：四个效果触发槽位

右手只有“效果映射”可以自定义。视觉效果本身是代码里预先实现并调好的固定成品。

每个槽位包含：

- 启用/停用。
- 触发手指选择：INDEX / MIDDLE / RING / PINKY。
- 固定效果库下拉选择。
- 10 秒以内的效果预览按钮或预览缩略状态。

明确禁止在设置页出现：

- 粒子数量、大小、速度调节。
- Shader 参数。
- 颜色调节。
- 恢复时间调节。
- X/Y 控制映射编辑。
- 节点图、代码编辑器或“创建自定义效果”。

这些参数可以在开发期间由代码作者调整，但不会暴露给拍摄者。

### 4.4 固定效果库

效果库由预先完成、经过测试的效果组成。可以在后续版本继续加入新的固定效果；加入后仍然只允许选择和分配，不允许在 UI 中编辑效果内部参数。

#### MVP 四个效果

1. `PARTICLE DISASSEMBLY`
   - 人物遮罩区域转为灰白粒子。
   - 触发点成为解构起点。
   - 保持捏合时，手的水平移动按固定规则影响扩散方向。
   - 松开后粒子重新聚合成人物。

2. `ASCII / DITHER`
   - 保持人物轮廓，内部材质转为 ASCII 与抖动网点。
   - 字符密度与明暗来自摄像头画面。
   - 手的纵向移动按固定规则切换稀疏与密集状态。
   - 松开后恢复灰白摄像头人物。

3. `RGB TIME ECHO`
   - 人物出现红、绿、蓝通道错位与时间拖影。
   - 手的移动方向按固定规则影响拖影方向。
   - 松开后拖影逐渐追上当前画面并归零。

4. `GLYPH DISSOLVE`
   - 人物先转为 dot matrix，再解构为字符材料。
   - 字符从触发点向外扩散。
   - 松开后字符重新组成 dot matrix，再恢复人物。

#### 后续固定效果

5. `DITHER`  
6. `DATA SLICE`  
7. `PIXEL SORT`  
8. `DOT MATRIX`  
9. `NEGATIVE THRESHOLD`  
10. `OPTICAL DISTORTION`

后续效果只有在实现、性能测试和许可证检查完成后才进入可选库；不允许只显示名称但使用占位动画。

### 4.5 默认右手映射

- INDEX → PARTICLE DISASSEMBLY
- MIDDLE → ASCII / DITHER
- RING → RGB TIME ECHO
- PINKY → GLYPH DISSOLVE

Jean 可以交换四个效果对应的手指，也可以用后续固定效果替换其中一个，但不能修改效果内部逻辑。

### 4.6 设置保存

localStorage 保存：

- 左手插槽启用状态、文字、TTS 配置与手指映射。
- 右手四个效果选择与手指映射。
- 上次选择的摄像头设备标识（在浏览器允许的范围内）。
- 追踪标记显示开关与声音总开关。

使用带版本号的数据结构，后续升级时可以迁移或安全重置。

### 4.7 进入表演页

点击 `ENABLE CAMERA →`：

1. 请求摄像头权限。
2. 加载 Hand Landmarker 与人物分割模型。
3. 显示明确的加载状态。
4. 成功后直接进入 `PERFORMANCE`。
5. 失败时停留在设置页并显示可行动的错误信息。

该点击同时构成浏览器声音激活手势，之后允许播放 TTS 与交互音效。

## 5. SCREEN 2 — PERFORMANCE

### 5.1 画面构成

- 全屏近黑背景。
- 摄像头画面水平镜像，行为与照镜子一致。
- 人物默认呈灰白、低饱和或单色状态。
- 画面主体是 Jean，而不是追踪界面。
- 追踪层只显示指尖、触发线和短暂坐标信息。
- 文字与效果必须渲染进最终合成画面，便于录屏或后续内部录制。

### 5.2 顶部工具栏

最终规格包含：

- `EDIT SETUP`：返回设置页并保留全部配置。
- `PHOTO 3·2·1`：三秒倒计时后导出当前合成画面。
- `REC 15s`：录制十五秒合成画面；属于后期功能，MVP 可先隐藏或标记未启用。
- `RESET`：停止当前文字、声音和效果，恢复默认画面，但不清空设置。
- `FULLSCREEN`：进入或退出浏览器全屏。

录制实际生产流程仍优先使用 OBS 或系统录屏。内置 `REC 15s` 不能阻塞手势识别与效果 MVP 的完成。

### 5.3 指尖追踪视觉

内部识别完整 21 个关键点，但画面只绘制双手各五个指尖：

- THUMB TIP
- INDEX TIP
- MIDDLE TIP
- RING TIP
- PINKY TIP

视觉规则：

- 默认指尖为克制的小红点。
- 不绘制手腕、掌心框、手指骨骼或 21 点编号。
- 拇指靠近另一根手指时，两点间出现细红线。
- 接近程度越高，线越亮、越完整。
- 捏合确认时，两点短暂合并为一个较大的红点。
- 右手效果触发时，在捏合中点短暂显示细红色 X/Y 坐标轴与简短效果名。
- 坐标轴和效果名在 300–600ms 后淡出，不持续占据画面。
- 左手文字触发时不显示多余技术标签，避免与内容竞争。

### 5.4 左右手识别

- 使用 MediaPipe handedness 区分 Left / Right。
- 摄像头画面镜像只影响显示和最终屏幕坐标转换，不允许把左右手业务映射颠倒。
- 左右手可以同时激活一个手势。
- 单只手同一时间只能激活一个捏合手势。
- 多根手指同时靠近拇指时，选择归一化距离最近者。

### 5.5 捏合状态机

每只手分别维护：

```text
IDLE
→ APPROACHING
→ ARMING
→ ACTIVE
→ RELEASING
→ IDLE
```

- `APPROACHING`：手指进入接近范围，显示红线。
- `ARMING`：进入触发阈值并开始约 200ms 计时。
- `ACTIVE`：保持时间达到要求后触发一次。
- `RELEASING`：距离超过释放阈值，效果进入恢复或文字淡出。
- `IDLE`：恢复等待状态。

距离必须用相对于手部尺度的归一化值计算，不能使用固定屏幕像素。

必须使用滞回阈值：

- `pinchOn`：进入捏合的较小阈值。
- `pinchOff`：退出捏合的较大阈值。

`pinchOff` 必须大于 `pinchOn`，避免临界距离反复触发。触发后增加短暂冷却时间，防止同一次捏合重复播放 TTS 或重复初始化效果。

### 5.6 左手表演行为

左手某个插槽进入 `ACTIVE` 时：

1. 从拇指与对应手指的中点生成文字。
2. 播放对应 TTS 或声音文件一次。
3. 保持捏合时，文字以平滑延迟跟随捏合中点。
4. 文字不是普通网页标签，必须进入最终视觉合成层。
5. 松开后文字保持短暂停留并淡出。

默认不让文字永久堆积。后续如需要“放下文字并留在画面”，作为独立模式增加，不混入 MVP。

### 5.7 右手表演行为

右手某个槽位进入 `ACTIVE` 时：

1. 在捏合中点显示短暂红色 X/Y 轴。
2. 显示极短的效果名确认，例如 `PARTICLE`。
3. 启动该槽位当前映射的固定效果。
4. 保持捏合时，手部运动按该效果代码中预设的规则控制动画。
5. 松开后按该效果固定的恢复曲线回到默认人物画面。

设置页不能编辑第 4、5 步的参数。

### 5.8 张开手与 RESET

- 张开右手：停止当前右手效果并恢复默认人物。
- 张开左手：不重新触发内容，当前文字按正常曲线淡出。
- `RESET`：立即取消双手状态、停止声音、清除文字、清除拖影缓存并恢复摄像头默认画面。
- RESET 不修改 localStorage 或手指映射。

## 6. 人物分割与效果作用范围

- 默认人物效果只作用于人物遮罩，不作用于背景。
- 背景默认保持近黑、稳定和低信息量。
- 人物遮罩边缘需要轻微羽化，避免硬切抖动。
- 遮罩短暂丢失时使用前一帧与时间平滑，不允许整个人突然消失。
- 明确标记为全画面效果的模式可以作用于背景，但 MVP 四个效果默认以人物为主体。

## 7. 声音

### 7.1 声音来源

- 浏览器 Speech Synthesis TTS。
- Jean 本地选择的音频文件。
- 固定的交互确认音。

### 7.2 声音规则

- TTS 或音频在手势第一次进入 ACTIVE 时播放一次。
- 保持捏合不重复播放。
- 同一个插槽重新触发前必须经过 RELEASE 与冷却。
- RESET 停止所有当前声音。
- 左右手视觉效果可有固定的轻量声音反馈，但不能压过左手语音。
- 所有声音只有在 `ENABLE CAMERA` 的用户点击之后启动。

### 7.3 录音限制

浏览器 TTS 不保证能直接写入 Canvas MediaRecorder 音轨，因此最终生产录制优先使用 OBS 或系统级录屏并确认系统声音被捕获。内置 `REC 15s` 在实现时必须单独验证音轨，不得假设 TTS 会自动被录入。

## 8. 视觉系统

### 8.1 色彩

- 背景：近黑。
- 摄像头人物：灰白、低饱和。
- 指尖、触发线、坐标轴：红色。
- 文字：灰白或接近白。
- 不使用蓝紫渐变、霓虹发光卡片或多色游戏界面。

### 8.2 字体与信息层级

- 技术信息使用克制的等宽字体。
- 用户自定义文字可以后续选择独立显示字体，但 MVP 先使用统一字体。
- 坐标与效果名尺寸小、出现时间短。
- 不使用大面积 HUD 边框。

### 8.3 动态原则

- 先让观众看见人物和手，再看见技术标记。
- 触发必须有“接近、确认、变化、恢复”完整节奏。
- 不允许效果瞬间硬切或松手立即归零。
- 所有恢复曲线由效果内部固定实现。

## 9. 数据模型

建议配置结构：

```ts
type Finger = 'index' | 'middle' | 'ring' | 'pinky';

type LeftSlot = {
  id: string;
  enabled: boolean;
  finger: Finger;
  text: string;
  audioMode: 'off' | 'tts' | 'file';
  ttsVoice?: string;
  ttsRate: number;
  ttsVolume: number;
};

type EffectId =
  | 'particle-disassembly'
  | 'ascii-dither'
  | 'rgb-time-echo'
  | 'glyph-dissolve'
  | 'dither'
  | 'data-slice'
  | 'pixel-sort'
  | 'dot-matrix'
  | 'negative-threshold'
  | 'optical-distortion';

type RightSlot = {
  id: string;
  enabled: boolean;
  finger: Finger;
  effectId: EffectId;
};

type FingercontrolConfig = {
  version: 1;
  leftSlots: LeftSlot[];
  rightSlots: RightSlot[];
  soundEnabled: boolean;
  trackingVisible: boolean;
};
```

注意：`RightSlot` 故意不包含效果参数。

## 10. 错误与降级

- 摄像头被拒绝：返回设置页并说明如何重新授权。
- 摄像头不存在或被占用：显示设备错误，不进入空白表演页。
- 模型加载失败：提供重试按钮。
- 只识别到一只手：该手继续工作，另一只手保持 IDLE。
- 人物分割性能不足：允许暂时降低内部遮罩分辨率，不关闭手势识别。
- 某个高级效果不受支持：从可选库隐藏，不使用假效果代替。
- WebGL 初始化失败：显示明确错误，不默默退化成与规格不符的静态滤镜。

## 11. 开源实现原则

- 优先研究成熟的 WebGL、GLSL、Three.js、Canvas 与 MediaPipe 开源实现。
- 每个采用或改写的来源必须记录 URL、许可证和修改范围。
- 不直接复制来源不明的 CodePen、演示站或压缩代码。
- 开源效果进入固定效果库前必须统一视觉语言和性能标准。
- 开源代码只是效果实现来源，不改变“效果写死、仅映射可选”的产品规则。

## 12. 分阶段构建

### Phase 1 — 识别骨架

- CONTROL SETUP 页面。
- 左右手共八个槽位。
- 摄像头权限与镜像。
- Hand Landmarker 双手识别。
- 五指尖红点。
- 捏合线、200ms 保持、滞回和冷却。
- 屏幕显示 `LEFT INDEX / SLOT TRIGGERED` 等调试确认。
- localStorage。

Phase 1 不实现人物分割和精致效果。

### Phase 2 — 表演合成

- 人物分割。
- 文字合成与 TTS。
- MVP 四个固定效果。
- X/Y 坐标轴。
- RESET 与 FULLSCREEN。
- 双手同时工作的状态管理。

### Phase 3 — 拍摄完善

- PHOTO 3·2·1。
- 评估并实现 REC 15s。
- 声音录制验证。
- 摄像头设备选择。
- 性能与画质调优。
- 增加后续固定效果。

### Phase 4 — 成片

- 确定最终左手文字与声音。
- 确定右手四个效果映射。
- 编排 10–15 秒表演动作。
- 使用 OBS 或系统录屏录制。
- 剪辑、调色、声音混合与无缝循环。
- 导出作品集首屏视频。

## 13. MVP 验收标准

### 设置页

- 四个左手内容插槽均可编辑并保存。
- 四个右手效果槽位均可交换手指与效果。
- 右手设置中不存在效果参数编辑器。
- 刷新页面后文本与映射恢复。

### 手势识别

- 同时识别双手。
- 左右手业务映射与镜像画面一致。
- 拇指分别与四根手指捏合都能稳定触发。
- 单次保持只触发一次。
- 临界位置不连续闪烁。
- 最终画面只显示五个指尖，不显示手腕和骨骼。

### 表演页

- 左手文字从正确接触点出现并随手移动。
- 左手声音只播放一次。
- 右手触发当前映射的正确固定效果。
- 松手后效果按固定曲线恢复。
- RESET 可以恢复干净状态。
- FULLSCREEN 正常工作。

### 效果

- MVP 四个效果不是同一个滤镜换名字。
- 人物效果默认只作用于人物遮罩。
- 不允许效果名称存在但实际只是占位动画。
- 画面风格符合近黑、灰白、红色追踪语言。

## 14. 最终首屏视频建议

工具支持八个手势，不代表成片必须展示八个功能。推荐最终 10–15 秒镜头：

1. Jean 在近黑背景中保持注视。
2. 左手捏合，生成一段自定义文字与声音。
3. 右手捏合，人物进入 PARTICLE DISASSEMBLY。
4. 双手运动让文字与人物在同一材料中变化。
5. 双手松开，人物重新聚合并回到循环第一帧。

文字内容、服装、构图和声音属于拍摄创意决策，不阻塞工具 Phase 1 的开发。
